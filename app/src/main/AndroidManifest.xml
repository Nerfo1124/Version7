<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.usuario.version7">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Vista.InicioSesion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Vista.Registro" />
        <activity android:name=".Vista.Inicio" />
        <activity android:name=".Vista.Conformula" />
        <activity android:name=".Vista.Sinformula" />
        <activity android:name=".Vista.Frecuencia" />
        <activity android:name=".Vista.Micuenta"></activity>
    </application>

</manifest>
